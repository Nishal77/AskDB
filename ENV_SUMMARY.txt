╔══════════════════════════════════════════════════════════════╗
║     AskYourDatabase - Environment Variables Summary          ║
╚══════════════════════════════════════════════════════════════╝

📁 FILES CREATED:
  ✅ .env.example (root)
  ✅ apps/api/.env.example
  ✅ apps/web/.env.example
  ✅ packages/prisma/.env.example

🔑 REQUIRED VALUES TO ADD:

1. OpenAI API Key
   File: apps/api/.env and .env
   Get: https://platform.openai.com/api-keys
   Format: OPENAI_API_KEY=sk-...

2. Database URL (Prisma)
   Files: apps/api/.env and packages/prisma/.env
   Format: DATABASE_URL="postgresql://user:pass@host:port/db?schema=public"
   Default: postgresql://postgres:postgres@localhost:5432/askyourdatabase

3. JWT Secret
   Files: apps/api/.env and .env
   Generate: openssl rand -base64 32
   Format: JWT_SECRET=your-32-char-secret

4. Web API URL
   File: apps/web/.env.local
   Default: NEXT_PUBLIC_API_URL=http://localhost:3000/api/v1

🚀 QUICK START:
   pnpm setup:env    # Create all .env files
   # Then edit and add your values
   pnpm dev          # Start application

📚 DOCUMENTATION:
   - ENV_REQUIREMENTS.md (complete list)
   - ENV_SETUP.md (detailed guide)
   - README_ENV.md (main guide)
   - ENV_QUICK_REFERENCE.md (quick ref)
